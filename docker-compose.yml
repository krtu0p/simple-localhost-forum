version: '3'
services:
  db:
    image: mysql:latest
    container_name: local-mysql-forum
    ports:
      - "3307:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=forum123
      - MYSQL_DATABASE=forum
    volumes:
      - ./db:/var/lib/mysql
